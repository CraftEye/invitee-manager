CREATE TABLE `prople` (
  `id` int,
  `name` varchar(255),
  `house_name` varchar(255),
  `phone_number` int,
  `notes` varchar(255),
  `mother_id` int,
  `father_id` int,
  `short_place` varchar(255),
  `place` varchar(255),
  `invited_ind` int
);
